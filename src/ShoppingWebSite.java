public interface ShoppingWebSite {

    // Interface is the kind of the class which will have every method as abstract method
    // We use interface to achieve abstraction
    // We use interface for designing purpose
   // Interface help us to achieve multiple inheritance in java
   public void orderItem();
   public void addToCart();
   public void showPrice();
   public void showProducts();
   public void showUserDetails();
}
